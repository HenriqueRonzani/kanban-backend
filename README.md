# Kanban Backend

## Requirements
- PHP 8.3
- Composer
- Docker

## Installation
1. Clone the repository
2. Run `composer install` to install dependencies
3. Copy `.env.example` to `.env` and configure your environment variables
4. Run `sail artisan key:generate` to generate the application key
5. Run `sail up` to start the application
